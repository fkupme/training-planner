# Тесты сто́ров (`src/stores/__tests__`)

Фокус: корректность сдвигов цикла, API сто́ров и агрегатов.

## Файлы
- `planner.cycle-shifting.test.ts` — проверка sparse rotation/сдвигов планировщика.
- `sessions.dayoffset.test.ts` — корректность `dayOffset` и переходов.
- `stats.api.test.ts` — поведение API статистики.
