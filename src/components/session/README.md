# Компоненты сессии (`src/components/session`)

UI для редактирования сетов, температуры, RPE и т.п. во время сессии.

## Файлы
- `SetEditorPopup.vue` — редактирование подходов/повторений/веса.

## Шаблон API
- props: текущий сет/упражнение, локальное состояние.
- emits: `save(set)`, `cancel()`.
