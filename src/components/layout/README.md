# Макеты (`src/components/layout`)

Базовые компоненты макета приложения.

## Файлы
- `AppLayout.vue` — корневой layout: контейнер, навигация, слот контента.

## Шаблон API
- slots: `default` — контент страницы.
